# OS / editor noise
.DS_Store
Thumbs.db
*.log
*.swp
*~
.vscode/
.idea/
*.sublime-*

# Node (used by .pot extractor + zip builder)
node_modules/
package-lock.json
# ...but DO commit the website's lock file for reproducible builds
!/website/package-lock.json

# Local-only working copies (not part of the shipped plugin)
quoted-payments-v0.1.0/
.docker-local/
*.local

# Build / release artefacts
quoted-*.zip
dist/
release/

# Backup / draft files
*.bak
*.orig
